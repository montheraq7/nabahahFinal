# 🚀 دليل النشر على Render

## الملفات المطلوبة ✅
تأكد من وجود هذه الملفات في مشروعك:
- `app.py` - تطبيق Flask الرئيسي
- `requirements.txt` - مكتبات Python المطلوبة
- `index.html` - واجهة المستخدم

## خطوات النشر على Render 📋

### 1. إنشاء حساب على Render
- اذهب إلى https://render.com
- قم بالتسجيل أو تسجيل الدخول

### 2. رفع المشروع إلى GitHub
```bash
# في مجلد المشروع
git init
git add .
git commit -m "Initial commit - Nabahah Risk Score API"
git branch -M main
git remote add origin [رابط مستودع GitHub الخاص بك]
git push -u origin main
```

### 3. إنشاء Web Service جديد على Render
1. اضغط على "New +" ثم اختر "Web Service"
2. اربط حساب GitHub الخاص بك
3. اختر المستودع الذي رفعت عليه المشروع

### 4. إعدادات النشر
املأ الحقول التالية:

**Name:** `nabahah-risk-api` (أو أي اسم تريده)

**Region:** اختر `Singapore` أو `Frankfurt` (الأقرب للسعودية)

**Branch:** `main`

**Root Directory:** اتركه فارغاً

**Runtime:** `Python 3`

**Build Command:**
```
pip install -r requirements.txt
```

**Start Command:**
```
gunicorn app:app
```

**Instance Type:** اختر `Free` للتجربة

### 5. متغيرات البيئة (اختياري)
لا حاجة لإضافة أي متغيرات بيئة حالياً

### 6. النشر 🎉
- اضغط على "Create Web Service"
- انتظر حتى ينتهي النشر (حوالي 2-5 دقائق)
- ستحصل على رابط مثل: `https://nabahah-risk-api.onrender.com`

## اختبار التطبيق ✅

### اختبار API:
افتح المتصفح واذهب إلى:
```
https://[اسم-التطبيق].onrender.com/api/health
```

يجب أن تحصل على:
```json
{
  "status": "healthy",
  "message": "Risk Score API is running",
  "model": "Random Forest",
  "training_samples": 5000
}
```

### اختبار الواجهة:
افتح:
```
https://[اسم-التطبيق].onrender.com
```

## ⚠️ ملاحظات مهمة

1. **الخطة المجانية:**
   - التطبيق قد يتوقف بعد 15 دقيقة من عدم الاستخدام
   - قد يستغرق 30-60 ثانية لإعادة التشغيل عند أول طلب

2. **تحديث التطبيق:**
   - كل push جديد على GitHub سيُحدّث التطبيق تلقائياً

3. **السرعة:**
   - السيرفر المجاني قد يكون بطيئاً في البداية
   - للحصول على أداء أفضل، استخدم الخطة المدفوعة

## 🔧 استكشاف الأخطاء

### إذا فشل النشر:
1. تحقق من Logs في Render Dashboard
2. تأكد من صحة ملف requirements.txt
3. تأكد من أن app.py لا يحتوي على أخطاء

### إذا كان API بطيء:
- هذا طبيعي في الخطة المجانية
- يمكنك الترقية إلى خطة مدفوعة للحصول على أداء أفضل

## 📊 معلومات المودل

- **النوع:** Random Forest Regressor
- **عدد الأشجار:** 100
- **نقاط التدريب:** 5000
- **دقة النموذج:** R² Score ≈ 0.98

### أهمية الميزات:
- المحاولات الفاشلة: 60%
- حساسية المعاملة: 19.5%
- شذوذ الوقت: 12.6%
- نوع الجهاز: 4.2%
- تطابق الموقع: 3.7%

## 📞 الدعم
إذا واجهت أي مشاكل، تحقق من:
- Render Logs
- Browser Console (F12)
- Network Tab في Developer Tools

---
تم إنشاؤه لهاكاتون أبشر طويق 🇸🇦
